{
  "name": "workshop-contract-test-2025",
  "version": "1.0.0",
  "description": "## 1. Provider",
  "main": "index.js",
  "scripts": {
    "provider:start": "node provider/provider-service.js",
    "provider:test": "jest provider/component-tests/ --coverage",
    "provider:verify": "pact-provider-verifier --provider MoviesAPI --provider-base-url http://localhost:3000 --pact-broker-base-url=$PACT_BROKER_BASE_URL --broker-username pact_workshop --broker-password pact_workshop --wait 10 --publish-verification-results true --provider-app-version 1.0.0 --branch=main --wait 10",
    "consumer:contract-test": "jest consumer/contract-tests/",
    "publish:pact": "pact-broker publish ./pacts --consumer-app-version=1.0.0 --branch=main --broker-base-url=$PACT_BROKER_BASE_URL --broker-username=pact_workshop --broker-password=pact_workshop"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.9",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "joi": "^17.13.3",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@pact-foundation/pact": "^13.2.0",
    "jest": "^29.7.0",
    "jsonschema": "^1.5.0",
    "supertest": "^7.0.0"
  }
}
